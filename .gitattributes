# Normaliza fim de linha apenas no que é realmente texto.
* text=auto

# Binários: nunca converter fim de linha (evita corromper o arquivo no clone).
*.pdf  binary
*.pptx binary
*.docx binary
*.zip  binary
*.png  binary
*.mp3  binary
*.mp4  binary
